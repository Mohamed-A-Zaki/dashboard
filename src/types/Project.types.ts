export type ProjectProps = {
  name: string;
  finish_date: string;
  client: string;
  price: string;
  status: string;
  progress: number;
  description: string;
  team: string[];
  tags: string[];
};
